# Doctor Data Scraper

This Python script uses Selenium to scrape doctor data from a specified website and save the extracted data to an Excel file.

## Installation

To install the necessary dependencies, run:

```bash
pip install selenium webdriver-manager pandas openpyxl
